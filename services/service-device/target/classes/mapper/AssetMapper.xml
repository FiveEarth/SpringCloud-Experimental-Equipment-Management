<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.graduation.device.dao.AssetDao">

    <resultMap id="AssetResult" type="edu.graduation.device.bean.Asset">
        <id column="id" property="id"/>
        <result column="equipment_id" property="equipmentId"/>
        <result column="asset_code" property="assetCode"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="listByEquipmentId" resultMap="AssetResult">
        SELECT * FROM lab_equipment_asset WHERE equipment_id = #{equipmentId} ORDER BY id
    </select>

    <select id="selectOneAvailable" resultMap="AssetResult">
        SELECT * FROM lab_equipment_asset
        WHERE equipment_id = #{equipmentId} AND status = 0
        ORDER BY id LIMIT 1
    </select>

    <select id="selectById" resultMap="AssetResult">
        SELECT * FROM lab_equipment_asset WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE lab_equipment_asset SET status = #{status} WHERE id = #{id}
    </update>

    <select id="countAvailableByEquipmentId" resultType="int">
        SELECT COUNT(*) FROM lab_equipment_asset WHERE equipment_id = #{equipmentId} AND status = 0
    </select>

    <insert id="insertBatch">
        INSERT INTO lab_equipment_asset (equipment_id, asset_code, status)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.equipmentId}, #{item.assetCode}, COALESCE(#{item.status}, 0))
        </foreach>
    </insert>

    <delete id="deleteByEquipmentIds">
        DELETE FROM lab_equipment_asset
        WHERE equipment_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
