<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.graduation.user.dao.UserDao">

    <select id="selectById" resultType="edu.graduation.user.model.SysUser">
        select id, username, password, real_name as realName, phone, email, status,
               create_time as createTime, update_time as updateTime
        from sys_user where id = #{id}
    </select>

    <!-- basic user info -->
    <select id="selectByUsername" parameterType="string"
            resultType="edu.graduation.user.model.SysUser">
        select
            id,
            username,
            password,
            real_name as realName,
            phone,
            email,
            status,
            create_time as createTime,
            update_time as updateTime
        from sys_user
        where username = #{username}
          and status = 1
    </select>

    <select id="selectByUsernameForAdmin" resultType="edu.graduation.user.model.SysUser">
        select id, username, password, real_name as realName, phone, email, status,
               create_time as createTime, update_time as updateTime
        from sys_user where username = #{username}
    </select>

    <!-- role codes for current user -->
    <select id="selectRoleCodesByUserId" parameterType="long"
            resultType="string">
        select
            r.role_code
        from sys_user_role ur
                 join sys_role r on ur.role_id = r.id
        where ur.user_id = #{userId}
    </select>

    <!-- permission codes for current user -->
    <select id="selectPermissionCodesByUserId" parameterType="long"
            resultType="string">
        select
            p.permission_code
        from sys_user_role ur
                 join sys_role_permission rp on ur.role_id = rp.role_id
                 join sys_permission p on rp.permission_id = p.id
        where ur.user_id = #{userId}
    </select>

    <select id="selectAll" resultType="edu.graduation.user.model.SysUser">
        select id, username, password, real_name as realName, phone, email, status,
               create_time as createTime, update_time as updateTime
        from sys_user
        <where>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by id
    </select>

    <insert id="insert" parameterType="edu.graduation.user.model.SysUser" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user (username, password, real_name, phone, email, status)
        values (#{username}, #{password}, #{realName}, #{phone}, #{email}, #{status})
    </insert>

    <update id="updateById" parameterType="edu.graduation.user.model.SysUser">
        update sys_user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where id = #{id}
    </update>

    <update id="updatePasswordById">
        update sys_user set password = #{password} where id = #{id}
    </update>

    <delete id="deleteById">
        delete from sys_user where id = #{id}
    </delete>

    <select id="selectRoleIdsByUserId" resultType="long">
        select role_id from sys_user_role where user_id = #{userId}
    </select>

    <delete id="deleteUserRolesByUserId">
        delete from sys_user_role where user_id = #{userId}
    </delete>

    <insert id="insertUserRole">
        insert into sys_user_role (user_id, user_name, role_id, role_name)
        values (#{userId}, #{userName}, #{roleId}, #{roleName})
    </insert>

    <update id="updateUserRoleUserName">
        update sys_user_role set user_name = #{userName} where user_id = #{userId}
    </update>

    <select id="selectRoleById" resultType="edu.graduation.user.model.RoleVO">
        select id, role_code as roleCode, role_name as roleName from sys_role where id = #{roleId}
    </select>

    <select id="selectAllRoles" resultType="edu.graduation.user.model.RoleVO">
        select id, role_code as roleCode, role_name as roleName from sys_role order by id
    </select>

</mapper>